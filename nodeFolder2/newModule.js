const user = require('./userfile');
console.log(user.getName() + ' lives in ' + user.getLocation() + 'and was born on ' + user.dob);

/**
 * the out put is
 * Jim lives in Mucichand was born on 12.01.1982
 * 
 */